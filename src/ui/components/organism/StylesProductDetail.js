import styled from "styled-components";

const Wrapper = styled.div`

h5, .h5 {
  font-size: 1.1rem;
}

.lighter-grey {
  color: rgba(0,0,0,.55);
  font-size: 14px;
  font-weight: 400;
}

`;

export default Wrapper;